import { Gem } from "lucide-react";

const BusinessBadge = () => (
  <span className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full bg-gradient-to-r from-amber-500/20 to-orange-500/20 border border-amber-500/30 text-amber-400 text-[11px] font-bold tracking-wide uppercase">
    <Gem size={12} />
    Elite
  </span>
);

export default BusinessBadge;
